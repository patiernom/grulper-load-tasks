{
  "name": "grulper-load-tasks",
  "version": "1.0.2",
  "description": "A simple module for load your gulp tasks",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/patiernom/grulper-load-tasks.git"
  },
  "keywords": [
    "gulp"
  ],
  "author": "Marco Patierno",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/patiernom/grulper-load-tasks/issues"
  },
  "homepage": "https://github.com/patiernom/grulper-load-tasks#readme",
  "main": "index.js",
  "scripts": {
    "test": "gulp test",
    "coveralls": "istanbul cover ./node_modules/mocha/bin/_mocha $(find tests -name '*.js') --report lcovonly -- -R spec && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage"
  },
  "engines": {
    "node": ">=0.10.22"
  },
  "dependencies": {
    "defaults": "^1.0.2",
    "glob": "^5.0.14"
  },
  "devDependencies": {
    "chai": "^3.2.0",
    "gulp-jscs": "^1.6.0",
    "gulp-jshint": "^1.11.2",
    "gulp-load-plugins": "^0.10.0",
    "gulp-istanbul": "^0.10.0",
    "gulp-mocha": "^2.1.3",
    "jshint-stylish": "^2.0.1",
    "mocha": "^2.2.5",
    "istanbul": "^0.3.17",
    "coveralls": "^2.11.2",
    "mocha-lcov-reporter": "0.0.2"
  },
  "peerDependencies":{
    "gulp": "^3.9.0"
  }
}
